spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MYSQL;DATABASE_TO_UPPER=false;NON_KEYWORDS=value;
    driver-class-name: org.h2.Driver
  cloud:
    discovery:
      enabled: false
  redis:
    redisKeyTimeToLive: 1
    port: 6379
    host: localhost

eureka:
  client:
    enabled: false

feign:
  client:
    enabled: false



